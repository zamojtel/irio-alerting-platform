# Frontend

Frontend uses React and TypeScript. It is bundled using Vite.

## Installation

To install the dependencies, run:

```bash
pnpm install
```

## Development

To start the development server, run:

```bash
pnpm dev
```
